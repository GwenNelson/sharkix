# The current known-good Sharkix machine preset. Its historical ordinary
# build is optimised; the name reserves this preset for the debug-capable PC.
ARCH ?= x86
ARCH_BITS ?= 64
PLATFORM_COMPONENTS ?= pc-generic pc-acpi
BOOT ?= multiboot1
